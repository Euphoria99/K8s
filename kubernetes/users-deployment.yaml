apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: users
  template:
    metadata:
      labels:
        app: users
    spec:
      containers:
        - name: users
          image: pavanbhat99/kub-demo-users:latest
          imagePullPolicy: Always
          env:
            - name: AUTH_ADDRESS
              value: localhost
        - name: auth
          image: pavanbhat99/kub-demo-auth:latest
          imagePullPolicy: Always
          #how we mention multiple containers in same pod
